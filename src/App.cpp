#include "App.h"

#include <iostream>

#include "Instance/VulkanWindow.h"

void App::run()
{

    // VulkanWindow::create_window(800, 800);
    // m_vulkan_instance = new VulkanInstance(
    //     "Awsome", {VK_EXT_DEBUG_UTILS_EXTENSION_NAME}, {"VK_LAYER_KHRONOS_validation"});
    // m_destructor_queue.add_to_queue([&] { delete m_vulkan_instance; });
    // VulkanWindow::create_surface(m_vulkan_instance->get_instance());
}
